{
  "name": "video-batch-download",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "description": "Douyin & Bilibili batch download & local transcription — Playwright + faster-whisper + OpenCC",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "download": "node scripts/download.mjs"
  },
  "dependencies": {
    "playwright": "^1.53.0"
  },
  "engines": {
    "node": ">=20"
  }
}
