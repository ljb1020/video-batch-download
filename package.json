{
  "name": "douyin-batch-download-skill",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Douyin batch download & local transcription — Playwright + faster-whisper + OpenCC",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "download": "node scripts/download.mjs"
  },
  "dependencies": {
    "playwright": "^1.53.0"
  },
  "engines": {
    "node": ">=20"
  }
}
