{
  "name": "video-batch-download",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "Pluggable multi-platform video download and local transcription",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "download": "node scripts/download.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "playwright": "^1.53.0"
  },
  "engines": {
    "node": ">=20"
  }
}
